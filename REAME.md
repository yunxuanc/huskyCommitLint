# 使用说明

```
npm i -D

# 修改代码后，提交
git add .
# 使用统一规范提交
npm run commit

# 或者直接将上面合并为
npm run commit-all

# 首次生成日志（不带格式）
npm run log

# 首次生成日志（带格式）
npm run release:first

# 非首次生成日志（带格式）
npm run release
```
